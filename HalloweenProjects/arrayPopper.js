/* 
Array Popper
Build a program that removes values from an array, alternating from the front and back each time you call your function. This should utilize OOP.

EX:
[1, 2, 3, 4, 5]
popper.pop() // 1
popper.pop() // 5
popper.pop() // 2
popper.pop() // 4
popper.pop() // 3
popper.pop() // undefined OR throw and error, or do nothing, whatever.
*/
